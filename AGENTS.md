# AI 编码代理工作约束

本文件供 Codex、Copilot 等编码代理以及所有开发者共同遵守。任何子目录说明可补充规则，但不能放宽本文件中的安全、数据和验证要求。

## 项目目标

在三周内交付“词元研究所”初步版本：支持 C 语言、Python、数据结构三门课程，每门首期约 40 个核心知识点，并跑通个性化学习、RAG辅导、代码练习、确定性验证、质量监督和画像更新流程。

## 不可改变的产品边界

1. 平台中心始终是计算机专业学习，不是财经问答平台；
2. 采用三个逻辑智能体：学情规划、课程辅导、质量监督；不要无理由增加智能体或拆成微服务；
3. 通用模型经 `apps/api/app/modules/model_adapters/` 接入科大讯飞 MaaS/Agent 平台；
4. Python 课程允许在讯飞 MaaS 上开展经审核的 SFT/LoRA 微调；训练数据、评测集、模型资源ID、费用确认和人工审核记录必须可追溯，C语言和数据结构继续使用通用模型；
5. RAG必须基于授权知识库并提供来源，禁止伪造引用；
6. 代码正确性由编译、测试和规则校验决定，语言模型只能解释结果；
7. “驼灵”API只用于课程后的授权、脱敏经管综合练习，且不得获得学生身份信息或底层敏感数据；
8. 未经批准不得扩大到生产级支付、社交、排行榜、高并发或全校课程覆盖。

## 架构边界

- `apps/web/`：Vue/Vite界面，不直接调用模型厂商或数据库；
- `apps/api/`：FastAPI模块化单体，对外提供统一API；
- `apps/api/app/modules/orchestration/`：流程状态与三个智能体协同；
- `apps/api/app/modules/rag/`：文档入库、检索、来源和引用校验；
- `apps/api/app/modules/learner_profile/`：画像、掌握度、计划和推荐；
- `apps/api/app/modules/practice/`：练习、Debug、代码运行与确定性验证；
- `apps/api/app/modules/model_adapters/`：所有外部模型适配、超时、重试和降级；
- `contracts/`：跨模块唯一公共契约来源；
- `course_packs/`：课程内容与评价规则，结构遵循统一模板；
- `apps/api/tests/`：后端、契约与跨模块测试；
- `apps/web/src/**/*.test.ts`：前端单元与交互测试；
- `infra/`：部署与运行环境，不存放业务规则。

禁止在页面、课程包或其他业务模块中直接调用讯飞/驼灵SDK；禁止复制一套旁路Schema绕开 `contracts/`。

## 开始任务前

1. 阅读关联Issue及验收标准；
2. 阅读将修改目录及 `docs/` 中相关规范；
3. 用搜索确认现有接口和实现，不重复造模块；
4. 声明准备修改的文件范围；
5. 如需改公共契约、数据库或依赖，先提示并单独处理。

## 实现要求

- 只完成Issue范围内的最小完整变更；
- 保持模块依赖单向，避免循环导入；
- 外部服务调用必须经过适配器，设置超时、有限重试、错误映射和可演示的降级结果；
- 所有模型输出先做Schema校验，再进入业务逻辑；
- 检索回答必须携带可核验的资料ID/片段ID，不足时明确“依据不足”；
- 学习画像更新记录依据和时间，不允许模型直接覆盖历史数据；
- 代码执行默认无网络、限制时间/内存/输出，并隔离文件系统；
- 新依赖必须说明必要性，优先复用现有依赖；
- 错误信息面向学生时清晰友好，日志中不出现秘密和敏感原文。

## 课程内容要求

- C、Python、数据结构均使用 `course_packs/_template/` 的相同结构；
- 知识点ID稳定且全局唯一，例如 `C-PTR-01`、`PY-FUNC-01`、`DS-TREE-01`；
- 内容包含目标、前置知识、难度、来源和测评映射；
- 代码题必须提供隐藏测试或可复现判定规则；
- 财经管理背景只作为应用题上下文，并标明 `scenario_scope: post_course_finance_practice`；
- 不适配财经的知识点（如C指针/内存）保持纯计算机基础训练，不强行包装。

## 验证与提交

完成修改后至少执行受影响范围的：

- 格式与静态检查；
- 单元测试；
- 公共契约/课程包Schema校验；
- 关键完整流程或接口测试。

无法运行某项检查时必须在PR中说明原因和人工验证方式，不得声称已通过。不要修改、删除或覆盖团队成员未关联本Issue的改动。

## AI 禁止事项

- 不得读取、输出或提交 `.env`、API Key、Token及凭据；
- 不得把真实姓名、学号、联系方式和完整学习日志发送到外部模型；
- 不得伪造教材、政策、财经案例来源或测试结果；
- 不得关闭安全检查、放宽代码沙箱限制来让演示通过；
- 不得执行破坏性数据库或文件操作；
- 不得在没有Issue和人工负责人的情况下大范围重构。
