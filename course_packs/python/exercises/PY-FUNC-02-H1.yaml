id: PY-FUNC-02-H1
title: 课后练习：计算加权成绩
course: python
schema_version: 0.1.0
version: 1
type: code
difficulty: beginner
estimated_minutes: 20
concept_ids:
- PY-FUNC-02
prompt: 定义 weighted_score(homework, exam, homework_weight=0.4)，返回加权成绩。读取作业分和考试分，输出结果并保留 1 位小数。
source_refs:
- SRC-PY-GUIDE-CURRICULUM
- SRC-PY-EXERCISM-TRACK
- SRC-PY-INTERACTIVE-TUTORIALS
evaluation:
  mode: tests
  starter_code: "def weighted_score(homework, exam, homework_weight=0.4):\n    # 考试权重为 1 - homework_weight\n\
    \    pass\n\nhomework, exam = map(float, input().split())\nprint(f'{weighted_score(homework, exam):.1f}')\n"
  runtime:
    language: python
    version: '3.11'
    entrypoint: main.py
    time_limit_ms: 2000
    memory_limit_mb: 128
    output_limit_kb: 64
    network_access: false
    filesystem_access: isolated
  tests:
  - id: public-normal
    visibility: public
    input: '80 90

      '
    expected_output: '86.0

      '
  - id: hidden-same
    visibility: hidden
    input: '70 70

      '
    expected_output: '70.0

      '
extensions:
  learning_stage: after_class
  audience: chinese_beginner
  scaffolding:
  - 默认参数写在形参列表中
  - 考试权重是 1 减作业权重
  - 函数返回数值，格式化在调用处完成
  input_format: 一行两个数字：作业分 考试分。
  output_format: 默认权重下的加权成绩，保留 1 位小数。
  constraints:
  - 0 <= 分数 <= 100
  - 默认作业权重为 0.4
  public_examples:
  - input: '80 90

      '
    expected_output: '86.0

      '
    explanation: 先按输入格式拆分数据，再逐步应用本节知识点；输出需与格式完全一致。
  reflection_prompt: 参数设计要让默认行为清晰，同时允许调用者显式覆盖。
  source_adaptation:
    source_id: SRC-PY-EXERCISM-TRACK
    source_scope: 'concept: parameters / return values / defaults'
    method: 保留能力目标与边界测试思路，使用中文重写题面、提示、样例和测试。
status: draft
