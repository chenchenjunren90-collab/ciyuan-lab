# 贡献指南

本项目采用“人对结果负责、AI辅助实现、契约先行、PR合并”的协作方式。普通成员在开发前必须阅读 `README.md`、`docs/member-workflow.md`、`AGENTS.md`、`docs/responsibilities.md`、`docs/collaborator-issues.md` 及本人负责模块的文档。

## 1. 分支模型

- `main`：稳定、可演示版本，仅从 `develop` 发布；
- `develop`：日常集成分支；
- `feat/<issue>-<topic>`：新功能；
- `fix/<issue>-<topic>`：缺陷修复；
- `content/<issue>-<course>`：课程知识点、练习、项目和来源；
- `test/<issue>-<topic>`：测试夹具、黄金集和验收用例；
- `docs/<issue>-<topic>`：纯文档修改；
- `chore/<issue>-<topic>`：构建、工具和维护任务。

禁止直接向 `main`、`develop` 推送。分支应从最新 `develop` 创建，保持短生命周期，避免多人长时间修改同一文件。

## 2. 从 Issue 到合并

1. 使用 `.gitee/ISSUE_TEMPLATE.zh-CN.md` 创建 Issue；
2. 明确负责人、文件范围、依赖接口和可验证的验收标准；
3. 涉及接口、事件或共享数据时，先提交 `contracts/` 变更；
4. 在独立分支开发，AI只能在 Issue 边界内辅助；
5. 完成测试、静态检查和课程包校验；
6. 使用 `.gitee/PULL_REQUEST_TEMPLATE.zh-CN.md` 发起 PR，并关联 Issue；
7. 普通成员的 PR 统一指派成员1·陈骏人验收和合并；普通成员不得自审或自合并；
8. 成员1自己的 PR 在全量检查、差异复核和验收证据齐全后可以自行验收；
9. 检查通过后以 squash 方式合并，删除功能分支。

## 3. 提交信息

使用简明的 Conventional Commits 风格：

```text
feat(learner): 增加知识掌握度更新
fix(rag): 修复引用编号与片段错位
content(python): 增加文件处理知识点
test(practice): 补充 C 语言超时用例
docs(architecture): 记录模型网关边界
```

一次提交只表达一项完整意图。不要提交密钥、个人数据、生成产物缓存或本地环境文件。

## 4. 契约优先

下列变化必须先更新 `contracts/` 和相关测试，再实现调用方：

- 对外 API 路径、请求体和响应体；
- 智能体输入输出结构；
- 学生画像、知识点、练习结果等共享 Schema；
- 课程包格式和版本；
- 错误码与状态枚举。

不兼容变更必须在 Issue 中说明迁移方案。数据库结构变化必须包含可回退的迁移，不允许只修改本地数据库。

## 5. 评审责任

| PR 作者 | 最终验收与合并规则 |
|---|---|
| 成员2—6 | 统一由成员1·陈骏人检查任务范围、契约、安全、测试和交付证据后合并 |
| 成员1·陈骏人 | 仅在差异逐项复核、全量检查通过、PR记录验收证据和回退方式后自行验收合并 |

领域负责人可以在 Issue 评论中提供课程事实、接口需求和测试依据，但不进入作者分支共同修改，也不形成第二个强制审核人。成员1对最终合并负责；发现专业事实无法确认时，应要求作者补证据或退回修改，而不是在评审中代写实现。

课程负责人完成本人课程的基础目录和交接包后，可以领取前端或 AI/RAG 的支援 Issue。支援 Issue 必须写明允许修改的具体文件；成员2或3仍负责提供领域接口和接收结果，最终 PR 仍由成员1验收合并。没有支援 Issue 时，课程负责人通过课程数据、Mock、黄金问答和验收样例协作，不直接修改其他人的主责目录。

## 6. AI Coding 规则

- 每次让AI工作前，提供 Issue、相关接口、允许修改的目录和验收命令；
- 要求AI先读取 `AGENTS.md` 以及目录内说明；
- AI不得决定产品范围、公共接口、安全策略和数据授权；
- AI生成的代码、测试、课程内容和引用均由提交者负责；
- PR中如实勾选AI辅助情况，并说明重点人工检查位置；
- 不向外部模型发送密钥、姓名、学号、原始学习日志、未授权课程资料或财经数据；
- 不接受“看起来正确”的模型判断替代编译、测试、Schema校验或人工核验。

## 7. 完成定义（Definition of Done）

PR满足以下条件才可合并：

- [ ] Issue验收标准逐项满足；
- [ ] 公共契约和文档已同步；
- [ ] 正常、异常、边界路径均有测试；
- [ ] 课程回答能返回有效来源，代码题有确定性测试；
- [ ] 不含密钥、真实个人信息和未经授权的数据；
- [ ] 日志不记录完整提示词、代码中的秘密或敏感响应；
- [ ] 普通成员 PR 已由成员1验收；成员1自己的 PR 已记录完整自检证据；
- [ ] 能在团队约定的本地/演示环境复现。

## 8. 冲突与决策

接口、数据边界或架构存在分歧时，先暂停相互冲突的实现，在 `docs/adr/` 新增简短决策记录。课程内容分歧以培养方案、课程大纲和经授权教材为依据；无法确认的内容标记待审，不交给模型自行裁决。
