# 六人岗位分工与 Issue 清单

本文件用于把“谁负责什么、交付给谁、什么算完成”固定下来。团队成员姓名尚未最终对应时，Issue、分支、PR 和会议记录统一使用 `成员1`—`成员6`；确定后只填写下方映射表，不改变角色边界。

## 1. 当前基线：这是可开发骨架，不是已完成产品

截至项目初始化，仓库已经具备统一目录、课程包模板、最小 OpenAPI/学习事件契约、前后端启动骨架、三智能体/RAG/画像/练习/模型适配端口、检查脚本和协作文档，但业务功能仍待实现：

- API 目前只有健康检查和能力清单；
- C、Python、数据结构课程 `manifest.yaml` 的 `implemented_core_concepts` 均为 `0`；
- 三个逻辑智能体、RAG、画像更新和代码验证当前主要是接口占位；
- 数据库、真实模型调用、完整学习闭环和三门课程内容需要六人按本文件并行完成。

因此，任何成员都不能把“目录存在”当作“功能完成”。完成必须同时提供实现、测试/校验、文档和可复现的演示证据。

## 2. 人员占位映射表

项目负责人在第一次分工会后填写此表，并同步各课程 `manifest.yaml` 的 `review.content_owner`。如果成员能力与建议角色不匹配，优先交换完整角色，不要把同一目录拆成多人共同主责。

| 固定编号 | 实际姓名 | Gitee 用户名 | 主角色 | 备份/缺席接手人 |
|---|---|---|---|---|
| 成员1 | 待填写 | 待填写 | 架构、后端底座与集成负责人 | 成员3 |
| 成员2 | 待填写 | 待填写 | 前端与学习体验负责人 | 成员1 |
| 成员3 | 待填写 | 待填写 | AI、RAG 与智能体负责人 | 成员1 |
| 成员4 | 待填写 | 待填写 | C 语言与代码验证负责人 | 成员6 |
| 成员5 | 待填写 | 待填写 | Python 课程负责人 | 成员4 |
| 成员6 | 待填写 | 待填写 | 数据结构与算法课程负责人 | 成员5 |

> 一项任务只能有一名主负责人。其他人可以协助、评审或提供输入，但不能出现“六个人共同负责”而无人对验收结果负责。

## 3. 六人总览

| 成员 | 一句话责任 | 默认主责目录 | 核心产物 |
|---|---|---|---|
| 成员1 | 让所有模块按照同一契约、数据模型和运行环境集成 | `contracts/`、`apps/api/app/api/`、`apps/api/app/core/`、`apps/api/app/modules/learner_profile/`、`infra/`、集成脚本 | 可从零启动的后端、数据库/画像规则、稳定契约、三课程完整流程集成与发布候选版 |
| 成员2 | 让学生在一个统一界面完成测评、学习、对话、练习和画像反馈 | `apps/web/` | 可演示的学生端；三门课程复用同一套组件；正常、加载、空数据和失败/降级状态完整 |
| 成员3 | 让模型回答有依据、输出有结构、故障可降级，三个智能体职责可验证 | `apps/api/app/modules/orchestration/`、`rag/`、`model_adapters/` | RAG 检索与引用、讯飞适配、驼灵受限适配、三智能体编排、Schema/安全门禁和 Mock 降级 |
| 成员4 | 建设 C 课程，并用确定性机制判断 C/Python 代码是否正确 | `course_packs/c/`、`apps/api/app/modules/practice/` | C 语言约40知识点；8—12个重点实践；固定版本、限时限资源的代码验证与 Debug 结果 |
| 成员5 | 建设 Python 课程和一个以编程能力为核心的脱敏经管综合练习 | `course_packs/python/` | Python 约40知识点；8—12个重点实践；分级题目、Debug 和至少一个课后综合练习 |
| 成员6 | 建设数据结构课程并复核算法、复杂度和测试设计 | `course_packs/data_structures/` | 数据结构约40知识点；8—12个重点实践；算法题、复杂度反馈和至少一个综合练习 |

“约40个知识点”是结构化目录目标：每个知识点至少有 ID、目标、前置、难度、来源和测评映射；重点的 8—12 个知识点再制作完整学习卡、代码题/Debug、递进提示和高质量反馈。

## 4. 各成员可执行职责

### 成员1：架构、后端底座与集成负责人

**主责内容**

- 维护公共 API、学习事件、共享 Schema、统一错误码与版本策略；
- 建立 PostgreSQL/pgvector、Redis、迁移、种子数据和学生内部标识规则；
- 实现画像持久化、基于证据的掌握度更新规则和下一任务所需的数据服务；
- 维护本地启动、环境配置、集成测试、CI 检查和 `develop` 演示候选版；
- 主持契约冻结和跨模块联调，解决跨模块冲突，但不代替其他成员完成其业务内容。

**接口输入/输出**

| 输入 | 输出 |
|---|---|
| 成员2提出的页面数据需求；成员3提出的智能体/RAG结构；成员4提出的 `VerificationResult`；三门课程包数据 | `contracts/openapi.yaml` 中评审通过的 HTTP JSON；`contracts/events/learning-event.schema.json` 格式的学习事件；可查询的画像/学习记录；统一错误与可启动环境 |

`LearningEvent.student_id` 只能使用平台内部不透明标识。公共契约先评审、后实现；成员1不能口头改变字段后要求其他人追赶。

**主要联调对象**：成员2（API消费者）、成员3（智能体/RAG）、成员4（代码验证）；课程数据问题分别找成员4/5/6。

**不得越界**：不得决定课程知识点和答案；不得把单门课程特殊逻辑写进公共 API；不得在 API 进程直接执行学生代码；不得未经使用方评审单方面改公共契约；不得成为所有任务都必须等待的“唯一编码者”。

### 成员2：前端与学习体验负责人

**主责内容**

- 实现统一学生端：课程选择、初始测评、计划、知识卡、RAG问答、代码/Debug、结果、画像与下一步；
- 根据 OpenAPI 建立统一请求层、类型和错误处理，先用 Mock 与固定夹具开发；
- 三门课程通过 `course_id` 和课程包配置复用组件，不复制三套页面；
- 明确加载、空数据、超时、依据不足、模型降级和代码运行失败状态；
- 准备演示路径与前端自动化/组件测试证据。

**接口输入/输出**

| 输入 | 输出 |
|---|---|
| `contracts/openapi.yaml`、课程元数据、计划/引用/验证/画像响应、成员1提供的 Mock | 符合契约的 HTTP 请求；可复现的页面状态；对 `source_id/chunk_id`、测试事实、推荐理由的清晰展示；前端错误与重试行为 |

前端只通过 HTTP API 访问后端，不直接导入后端模块，不直接连接数据库，也不直接调用讯飞或驼灵。

**主要联调对象**：成员1（API与数据）、成员3（模型状态/引用）、成员4（代码验证反馈）、成员4/5/6（课程展示抽查）。

**不得越界**：不得在页面硬编码知识点、答案和课程特例；不得由前端判断代码是否通过；不得在浏览器保存模型密钥；不得绕过 `contracts/` 自创字段；不得为了好看隐藏失败、依据不足或降级状态。

### 成员3：AI、RAG 与智能体负责人

**主责内容**

- 实现授权资料的入库、分块、检索、来源回传和引用校验；
- 通过 `model_adapters/` 统一接入讯飞 MaaS/Agent，提供超时、有限重试、错误映射、Mock 和固定降级结果；
- 仅在授权、脱敏的课后经管综合练习中适配驼灵 API；
- 实现学情规划、课程辅导、质量监督三个逻辑智能体的结构化编排，不拆成三个服务；
- 校验模型输出 Schema、引用、敏感字段，以及反馈与确定性测试是否一致。

**接口输入/输出**

| 端口/输入 | 必须输出 |
|---|---|
| `KnowledgeRetriever.search(query, course_id, top_k)` | `SearchHit[]`，每条含 `source_id`、`chunk_id`、`content`、`score`、`metadata` |
| `ModelAdapter.complete(messages)` | 归一化 `ModelResponse`，含 `content`、`provider`、`model`、`usage`，异常转为统一错误或降级结果 |
| `LearningOrchestrator.next_activity(student_id, course_id)` + 画像、课程 ID、练习事实 | `PlannedActivity` 或经契约确认的结构化计划/辅导结果，只引用存在的知识点 ID 和可核验来源 |

**主要联调对象**：成员1（契约、画像与日志）、成员2（引用/状态展示）、成员4（测试事实）、成员4/5/6（课程来源和内容抽查）。

**不得越界**：不得让模型直接写数据库画像；不得让模型覆盖编译/测试结论；不得为每门课程复制智能体；不得伪造引用或把“模型知道”当来源；不得把姓名、学号、完整学习日志、密钥或未授权资料发给外部模型；不得把驼灵扩展成财经问答中心。

### 成员4：C 语言与代码验证负责人

**主责内容**

- 按统一模板建设 C 语言知识点、来源、测评、代码题和 Debug 任务；
- 实现 `practice/` 中与语言无关的验证编排，以及 C/Python 固定版本的编译/运行、测试映射和诊断；
- 与成员1共同定义隔离运行环境、无网络、超时、内存、输出和文件系统限制；
- 为正确、编译错误、运行错误、答案错误、超时和资源超限准备固定测试；
- 支持成员5/6复用验证能力，不为 C 单独复制一套业务流程。

**接口输入/输出**

| 输入 | 输出 |
|---|---|
| `language`、`source_code`、`limits`、练习的公开/隐藏测试和判定规则 | `VerificationResult { accepted, passed_tests, total_tests, diagnostics }`；诊断必须脱敏、限长、可复现 |

**主要联调对象**：成员1（运行隔离、API与日志）、成员2（结果展示）、成员3（基于测试事实生成反馈）、成员5（Python测试）、成员6（算法测试）。

**不得越界**：不得在 FastAPI 进程或宿主机无隔离执行学生代码；不得开放网络或任意文件访问；不得把语言模型作为代码裁判；不得为方便演示取消资源限制；不得强行给指针、内存等基础知识套财经背景；不得修改其他课程包内容。

### 成员5：Python 课程负责人

**主责内容**

- 按统一模板建设 Python 知识点、前置关系、学习目标、来源和测评映射；
- 选择8—12个重点知识点制作分级练习、代码题、Debug、递进提示和反馈要点；
- 建设至少一个课后综合练习，可使用授权/合成的脱敏经管数据背景，但评价核心仍是 Python 编程与数据处理；
- 向成员4提供可复现的公开/隐藏测试和资源要求；
- 审核 C 课程结构与表达，协助审核数据结构课程可读性。

**接口输入/输出**

| 输入 | 输出 |
|---|---|
| 培养方案、课程大纲、授权教材/资料、`course_packs/_template/`、代码验证规则 | `course_packs/python/` 下的 `manifest.yaml`、`concepts/`、`exercises/`、`projects/`、`sources/`；知识点 ID 使用 `PY-*`，来源可追溯，练习可判定 |

**主要联调对象**：成员4（代码测试与C互审）、成员6（算法/复杂度复核）、成员3（入库与来源）、成员2（页面抽查）。

**不得越界**：不得为 Python 新建专用后端或智能体；不得让财经背景压过编程目标；不得使用真实未授权业务数据；不得用模型评分替代确定性代码测试；不得把模型生成内容未经来源核验直接标为 `reviewed`；不得修改 C/数据结构课程包。

### 成员6：数据结构与算法课程负责人

**主责内容**

- 按统一模板建设线性表、栈队列、树、图、查找、排序、复杂度等知识点及前置关系；
- 选择8—12个重点知识点制作算法题、Debug、边界用例、复杂度反馈和递进提示；
- 设计至少一个多知识点综合练习；财经/管理场景只在自然适配时使用；
- 复核 C/Python 练习中的算法结论、复杂度表述和边界测试；
- 与成员4统一算法题的测试输入、时间/内存限制和诊断分类。

**接口输入/输出**

| 输入 | 输出 |
|---|---|
| 培养方案、课程大纲、授权教材/资料、统一课程模板、验证器限制 | `course_packs/data_structures/` 下完整课程包；知识点 ID 使用 `DS-*`；算法题具备可复现测试、边界用例和复杂度期望 |

**主要联调对象**：成员4（算法验证）、成员5（课程结构互审）、成员3（RAG来源）、成员2（页面抽查）。

**不得越界**：不得为每种数据结构开发独立页面/后端流程；不得只给答案而缺少边界与复杂度依据；不得使用无来源算法材料；不得以理论正确替代可运行测试；不得修改 C/Python 课程包。

## 5. 目录所有权与跨目录规则

| 路径 | 主负责人 | 修改规则 |
|---|---|---|
| `contracts/**` | 成员1 | 任意成员可提变更PR；合并前必须由成员1和至少一名实际使用方评审 |
| `apps/web/**` | 成员2 | 跨模块成员不直接改页面；通过Issue和契约提出需求 |
| `orchestration/**`、`rag/**`、`model_adapters/**` | 成员3 | 课程负责人提供资料与验收样例，不复制适配器或提示词 |
| `learner_profile/**`、API/Core、`infra/**` | 成员1 | 画像规则需成员3确认输入；代码隔离需成员4参与评审 |
| `practice/**` | 成员4 | 公共响应变更先走 `contracts/`；安全边界由成员1+3评审 |
| `course_packs/c/**` | 成员4 | 内容评审人为成员5；算法内容增加成员6评审 |
| `course_packs/python/**` | 成员5 | 内容评审人为成员4；算法内容增加成员6评审 |
| `course_packs/data_structures/**` | 成员6 | 内容评审人为成员5；测试机制增加成员4评审 |
| `_template/**`、校验脚本、根配置 | 成员1 | 属于共享标准；变更必须同步三门课程并通过全部校验 |

跨目录修改必须在 Issue 中写明原因，并在开始前通知该目录主负责人。紧急联调也不能把别人的目录当作“顺手修一下”；可以提交最小修复 PR，但必须由目录负责人评审。

## 6. 固定互审与联调关系

### 必须评审

- 成员1的公共 API/数据变更：成员2（前端消费）和对应领域成员至少一人评审；
- 成员2的学生主流程：成员1评审接口一致性，课程页面由对应课程负责人抽查；
- 成员3的模型/RAG/智能体变更：成员1评审契约与日志，涉及课程来源时由对应课程负责人评审；
- 成员4的验证器：成员1评审隔离与资源限制，成员3评审模型反馈不得篡改测试事实；
- C ↔ Python：成员4和成员5互审课程结构、表达与测评可执行性；
- 数据结构：成员5审结构与教学表达；成员6复核 C/Python 中的算法和复杂度内容。

### 联调规则

- 联调只以远程 `develop` 最新提交为基线，不通过聊天软件传递代码包；
- 每人同时只能有一个“进行中”编码Issue，完成或交付评审后再领取下一个；
- 外部模型不可用时必须能切换到Mock/降级路径，不能阻断核心学习闭环；
- 出现阻断性接口分歧时暂停冲突实现，先在Issue或ADR中形成结论；
- 任何人不得审核并合并自己的PR，阻塞的PR由成员1重新指定评审人。

## 7. Gitee 完整待办池

下表是当前初步版本的完整待办池，不代表实施先后顺序，也不包含完成日期。表中的标识是Issue标题前缀，不是Gitee自动编号；创建后将实际Issue链接回填到看板。每个Issue只能有一名主负责人，验收标准应原样复制到Issue描述并逐项勾选。

| 标识与Issue标题 | 主负责人 | 协作/评审 | 验收标准 |
|---|---|---|---|
| `ARCH-01 定义最小学习闭环OpenAPI与学习事件` | 成员1 | 成员2、3、4 | 契约覆盖测评、计划、学习/问答、练习验证、画像更新与推荐；示例请求/响应可校验；不兼容变更有说明 |
| `ARCH-02 完善统一课程包Schema与校验规则` | 成员1 | 成员4、5、6 | `_template` 明确知识点、练习、项目、来源字段；三个课程包使用同一版本；非法ID、前置环和无来源内容可被拒绝 |
| `DATA-01 建立课程、画像与学习记录最小数据模型` | 成员1 | 成员3、4 | 本地数据库可启动；迁移可重复执行；使用内部学生ID；课程、掌握度和学习事件可保存/查询 |
| `DATA-02 实现基于证据的画像更新与推荐数据服务` | 成员1 | 成员3、4 | 画像更新记录依据、时间和版本；同一事件不重复计分；模型不能直接覆盖分数；可返回推荐所需数据 |
| `DEVOPS-01 完成本地一键启动与自动检查` | 成员1 | 全员 | README命令可从干净环境启动；前后端、PostgreSQL和Redis健康可见；检查脚本返回明确成功/失败；无密钥入库 |
| `INTEGRATION-01 跑通Mock模式完整学习闭环` | 成员1 | 全员 | 从测评到下一任务可复现；每个模块都有固定输入/输出；任一外部模型缺失时流程仍可完成 |
| `INTEGRATION-02 建立三课程端到端验收测试` | 成员1 | 成员2、4、5、6 | C、Python、数据结构各有一条自动或可复现流程；接口、课程ID和结果结构一致；失败证据可定位 |
| `RELEASE-01 准备演示环境、种子数据与恢复方案` | 成员1 | 成员2、3 | 演示环境可部署；账号与数据脱敏；模型配额/降级开关可检查；故障后能回到可演示状态 |
| `WEB-01 建立学生端路由、统一请求层与Mock状态` | 成员2 | 成员1 | 页面覆盖主流程入口；请求类型符合OpenAPI；加载、空数据、错误和Mock状态可复现；无直连数据库/模型 |
| `WEB-02 实现课程目录、知识卡与来源展示` | 成员2 | 成员3、4、5、6 | 三门课程共用组件；知识点与前置关系可浏览；来源ID/片段可查看；无课程内容硬编码 |
| `WEB-03 实现初始测评、学习计划与推荐交互` | 成员2 | 成员1、3 | 可提交测评并显示结构化计划、推荐理由和掌握度；非法/缺失数据有明确提示；不在前端自行算分 |
| `WEB-04 实现RAG问答、引用与依据不足状态` | 成员2 | 成员3 | 对话显示引用；依据不足、超时和模型降级状态明显；不能把无来源内容伪装成确定答案 |
| `WEB-05 实现代码编辑、提交、Debug与验证反馈` | 成员2 | 成员4 | 支持C/Python代表性题目；展示测试事实和受控诊断；超时/资源超限可区分；前端不判定是否通过 |
| `WEB-06 实现学习画像、下一任务与完整异常状态` | 成员2 | 成员1、3 | 行为后画像变化和依据可见；下一任务可进入；网络/模型/代码运行失败均有恢复操作；三课程表现一致 |
| `AI-01 实现讯飞模型适配器与Mock降级` | 成员3 | 成员1 | 输出统一为 `ModelResponse`；正常、超时、限流、无配置均有测试；密钥只读环境变量；日志不含敏感原文 |
| `RAG-01 实现课程资料入库、检索与引用` | 成员3 | 成员4、5、6 | 仅处理登记来源；返回 `source_id/chunk_id` 和分数；三门课程各有可核验样例；依据不足明确降级 |
| `AGENT-01 实现学情规划智能体结构化输出` | 成员3 | 成员1 | 只引用现有知识点ID；计划符合Schema；输入含画像依据；无数据时返回受控计划而非编造历史 |
| `AGENT-02 实现课程辅导智能体的引导式反馈` | 成员3 | 成员4、5、6 | 讲解/提示基于RAG与练习事实；默认不直接泄露完整答案；引用缺失可拒答；三课程复用同一流程 |
| `AGENT-03 实现质量监督智能体与输出门禁` | 成员3 | 成员1、2、4 | 可检查Schema、引用、敏感字段和测试一致性；发现冲突可拒绝/降级；不能修改确定性测试结果 |
| `TUOLING-01 实现驼灵受限场景适配` | 成员3 | 成员1、5、6 | 只接受 `post_course_finance_practice`；只发送授权脱敏数据；关闭后综合练习仍可使用固定合成背景 |
| `PRACTICE-01 建立C/Python确定性代码验证链路` | 成员4 | 成员1、3、5 | 返回标准 `VerificationResult`；覆盖通过、编译/运行错误和答案错误；固定语言版本；禁止在API进程执行代码 |
| `PRACTICE-02 完善隐藏测试、资源限制与安全诊断` | 成员4 | 成员1、3、6 | 无网络并限制时间、内存、输出和文件访问；覆盖超时/超限；隐藏输入不泄露；诊断限长且可复现 |
| `CONTENT-C-01 建立C语言约40知识点目录与来源` | 成员4 | 成员5、6 | ID稳定且前置无环；每点有目标、难度、来源和测评映射；校验通过；不适配内容不强行财经化 |
| `CONTENT-C-02 完成C语言8—12个重点实践` | 成员4 | 成员5、6 | 每个重点点含学习卡、分级练习、代码或Debug任务、递进提示和确定性判定；来源已互审 |
| `CONTENT-C-03 完成C语言综合练习与演示路径` | 成员4 | 成员1、2 | 综合练习覆盖多个知识点；正确/错误路径可演示；验证结果影响画像和推荐；无需财经包装也成立 |
| `CONTENT-PY-01 建立Python约40知识点目录与来源` | 成员5 | 成员4、6 | ID使用 `PY-*` 且前置无环；每点有目标、难度、来源和测评映射；校验通过 |
| `CONTENT-PY-02 完成Python 8—12个重点实践` | 成员5 | 成员4、6 | 每个重点点含学习卡、分级题、代码/Debug、提示和可复现测试；模型生成内容已人工核验 |
| `CONTENT-PY-03 完成Python脱敏经管综合练习与演示路径` | 成员5 | 成员3、4 | 使用合成/授权脱敏数据；评价核心为Python编程与数据处理；驼灵关闭时可降级；完整闭环可演示 |
| `CONTENT-DS-01 建立数据结构约40知识点目录与来源` | 成员6 | 成员5、4 | ID使用 `DS-*` 且前置无环；每点有目标、难度、来源和测评映射；覆盖核心结构与算法 |
| `CONTENT-DS-02 完成数据结构8—12个重点实践` | 成员6 | 成员4、5 | 算法题含边界用例、复杂度期望、代码/Debug和确定性测试；表述经互审；校验通过 |
| `CONTENT-DS-03 完成数据结构综合练习与演示路径` | 成员6 | 成员2、4 | 覆盖多个结构/算法；可展示正确性、复杂度和错误反馈；结果进入画像与下一任务推荐 |
| `QA-01 建立契约、课程包和跨模块自动校验` | 成员1 | 全员 | OpenAPI、学习事件、课程包Schema和关键端口有自动检查；失败提示可定位文件/字段；纳入统一检查脚本 |
| `QA-02 完成安全、降级与边界测试` | 成员1 | 成员2、3、4 | 覆盖提示注入、敏感字段、无引用、模型超时、代码超时/超限和日志脱敏；不得为通过测试关闭安全限制 |
| `DOC-01 整理架构、使用说明、演示脚本与已知限制` | 成员1 | 全员 | README与实现一致；每位成员补充负责模块说明；三课程演示步骤可复现；未完成功能和限制如实列出 |

从待办池领取任务时，按照依赖关系选择当前可执行项；若发现新工作，先确认无法纳入现有Issue，再新增同样具有负责人、评审人和验收标准的Issue，禁止用一个“大而全”的Issue承载多人并行修改。

## 8. 每个人的完成定义

一项任务只有同时满足以下条件才算完成：

- Issue验收项逐条有证据，不以“我写完了”作为状态；
- 修改范围符合目录所有权，公共契约和文档已同步；
- 正常、失败和边界路径至少各有一个测试/可复现检查；
- 课程内容有授权来源和另一课程负责人审核，代码题有确定性判定；
- PR由指定互审成员通过，已在最新 `develop` 上联调；
- 演示所需Mock/降级路径可用，不依赖某个人电脑或临时密钥；
- 不含真实个人信息、密钥、未授权资料或未经脱敏的财经数据。

## 9. 人员变化处理

本版本按六人设计。若有人短期缺席，先由映射表中的备份人接手阻断集成的Issue，非关键内容顺延；若长期变为五人，优先压缩每门重点实践数量而不是取消课程统一标准、确定性验证或安全边界。任何合并角色都要在Gitee Issue中写明新的主负责人和缩减范围。
